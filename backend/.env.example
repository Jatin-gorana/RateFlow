# Server Configuration
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# For production deployment, set FRONTEND_URL to your deployed frontend URL:
# FRONTEND_URL=https://rateflow-kappa.vercel.app

# Database Configuration (Optional - using mock data for now)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=yield_tracker

# Redis Configuration (Optional - using in-memory cache for now)
REDIS_URL=redis://localhost:6379

# Blockchain Configuration (Required for real Aave data)
ETHEREUM_RPC_URL=https://eth-mainnet.alchemyapi.io/v2/YOUR_ALCHEMY_API_KEY

# Alternative RPC providers (choose one):
# ETHEREUM_RPC_URL=https://mainnet.infura.io/v3/YOUR_INFURA_PROJECT_ID
# ETHEREUM_RPC_URL=https://rpc.ankr.com/eth

# Polling Configuration
POLLING_INTERVAL=45000

# Logging
LOG_LEVEL=info